{
  "name": "@linguaflow/offline-sync",
  "version": "0.1.0",
  "private": true,
  "description": "Offline-first data sync engine (Phase 3)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@linguaflow/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^2.0.0"
  }
}
